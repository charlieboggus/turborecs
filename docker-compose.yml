services:
  db:
    image: postgres:16
    env_file:
      - ./.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks: [internal]
    # no ports:

  api:
    build:
      context: ./api
    env_file:
      - ./.env
    depends_on:
      - db
    networks: [internal]
    # no ports:

  web:
    build:
      context: ./web
    depends_on:
      - api
    networks: [internal]
    environment:
      API_INTERNAL_URL: http://api:8080
      API_INTERNAL_TOKEN: ${INTERNAL_AUTH_TOKEN}
      API_ADMIN_TOKEN: ${ADMIN_AUTH_TOKEN}
    ports:
      - "3000:3000"

volumes:
  pgdata:

networks:
  internal:
    driver: bridge